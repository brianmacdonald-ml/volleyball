
function execute() {
	
	
	setTimeStamps();
	
}
/*
 * We need to set up an object that will have name & version properties and 
 * have an execute() function
 */
var statType = {
	name:         "Highlight",
	friendlyName: "The play before was a highlight!",
	version:      1.0,
	execute:      execute,
};